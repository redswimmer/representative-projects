You are an analyst advising a job applicant. Below are the problems a company is hiring to solve — extracted from the "{{listing_id}}" job listing, each grounded in verbatim quotes — followed by the listing itself.

Propose exactly ONE small representative project the applicant could build and show the company as evidence they can solve these problems.

Rules:
- One project, decomposed into components. Each component names the problem ids it addresses ("addresses") and gives a one-sentence justification for why that component is convincing evidence against those problems.
- Every problem must be addressed by at least one component.
- roadmap: ordered milestones for building the project; each milestone lists the component ids it delivers. Reference only component ids that exist.
- Framing and direction only — NEVER include code, shell commands, or snippets. The applicant does the building.
- Respond with JSON only. No markdown fences, no commentary. Use exactly this shape:
{"project": {"title": "...", "summary": "...", "components": [{"id": "C1", "name": "...", "description": "...", "addresses": ["P1"], "justification": "..."}], "roadmap": [{"milestone": "...", "components": ["C1"]}]}}

Problems:
{{problems}}

Job listing:

{{listing}}
