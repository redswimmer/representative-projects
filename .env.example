# Copy to .env and set your key (promptfoo loads .env automatically)
OPENAI_API_KEY=sk-your-key-here

# Keep eval results local — never upload to promptfoo's sharing service
PROMPTFOO_DISABLE_SHARING=true

# Optional: disable anonymous usage telemetry
# PROMPTFOO_DISABLE_TELEMETRY=1
